# 方案 / Adoption Plan — <project or task name>

> Fill every section unless the routed scenario says to omit/shrink one. Mark
> inferred values with *(inferred)* and unknowns with *(unverified)*. Write in
> the user's language (default 中文). Keep tool names and version refs verbatim.
> Do not invent commands — link to each tool's docs.

## 0. 概况 / Situation (as understood)
- **Scenario / 场景**: <1–4 + label>
- **Situation profile / 处境画像**:
  | Axis | Value | Source |
  |---|---|---|
  | greenfield vs brownfield | … | *(inferred / asked)* |
  | stack / languages | … | … |
  | existing AI tooling | … | … |
  | test maturity | … | … |
  | team vs solo | … | … |
  | primary goal | … | asked Q1 |
  | risk tolerance | … | asked Q2 |
  | multi-model available | … | asked Q3 |
- _Inferred values are marked; corrections welcome._

## 1. 推荐组合 / Recommended stack
| Layer | Tool | Version (registry) | Why this **layer** | Why this **tool** |
|-------|------|--------------------|--------------------|-------------------|
| base-standard | … | … *(or unverified)* | <axis that drove it> | <registry `strengths`> |
| spec | … | … | … | … |
| … | … | … | … | … |

- **故意排除 / Deliberately excluded**: <layer or tool> — <reason>
  (shows restraint; prevents over-stacking).

## 2. 上手步骤 / How to set up & use
Ordered, smallest-reversible-first. For each tool, link `source.repo` /
`source.homepage` from the registry; **do not invent commands** — point to the
tool's own docs.
1. <first reversible step — e.g. add an `AGENTS.md`>
2. <adopt on ONE feature / slice first>
3. <the daily usage loop with this stack>

## 3. 注意事项与风险 / Caveats & risks
- <tool-specific weaknesses from registry `weaknesses`>
- <anti-patterns this stack could fall into + how to avoid> — e.g. spec drift ⇒
  review specs every PR.
- <security signals from registry `security_signals`, if any>

## 4. Token / 成本 / Token & cost notes
- Expected token posture of this stack (low / medium / high) and why.
- Specific cost traps avoided (e.g. no orchestration layer ⇒ no per-call skill/MCP injection blow-up).

## 5. 维护姿态 / Maintenance posture
- How to keep it healthy long-term (sync specs, prune unused skills/hooks, re-review quarterly).
- _(Omit or shrink for one-off tasks.)_

## 6. 备选方案 / Fallbacks
- **更轻 / Lighter**: <a smaller stack> — when budget/time is tighter.
- **更重 / Heavier**: <a bigger stack> — when this proves insufficient.

## 7. 下一步 / Next steps
- The single first action.
- The checkpoint at which to re-evaluate, and the signal that would trigger a switch.

---
_Generated by **code-agent-toolsmith** skill `v<X.Y.Z>`, registry snapshot
`v<…>`. Values marked "unverified" were not confirmed against upstream._
